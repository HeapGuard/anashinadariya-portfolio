import type { Project } from "../../data/portfolio";
export function ProjectPreview({ project }: { project: Project }) { return <><div className="project__number">{String(project.index + 1).padStart(2, "0")}</div><div className="project__metadata"><p className="project__type">{project.type}</p><h3 id={`project-${project.className.replace("project--", "")}-title`}>{project.title.map((line) => <span key={line}>{line}<br /></span>)}</h3>{project.description && <p>{project.description}</p>}</div><figure className="project__image"><img src={project.image} alt={project.imageAlt} /></figure></>; }
