import { FiArrowUpRight } from "react-icons/fi";
import { navigation } from "../../data/portfolio";
export function SiteNavigation({ className = "" }: { className?: string }) { return <nav className={className} aria-label="Основная навигация">{navigation.map((item, index) => <a href="#works" key={item}>0{index + 1} / {item}</a>)}</nav>; }
export function MobileMenuButton({ open, onToggle, className }: { open: boolean; onToggle: () => void; className: string }) { return <button className={className} type="button" aria-label={open ? "Закрыть меню" : "Открыть меню"} aria-expanded={open} onClick={onToggle}><span /><span /></button>; }
export function CompactHeader({ visible, open, onToggle }: { visible: boolean; open: boolean; onToggle: () => void }) { return <header className={`compact-header ${visible ? "compact-header--visible" : ""}`} aria-label="Закреплённая навигация"><a className="hero-name" href="#top">DARIA<br />ANASHINA</a><SiteNavigation className={open ? "compact-navigation compact-navigation--open" : "compact-navigation"} /><a className="compact-header__cta" href="#works">РАБОТЫ <FiArrowUpRight aria-hidden="true" /></a><MobileMenuButton className="compact-menu-button" open={open} onToggle={onToggle} /></header>; }
